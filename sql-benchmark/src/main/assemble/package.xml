<assembly>
    <id>${artifactId}</id>
    <formats>
        <format>zip</format>
    </formats>
    <!--如果为false，不会额外产生根目录，否则，在打好的包中会产生以pom.xml中artifactId和version命名的根目录-->
    <includeBaseDirectory>false</includeBaseDirectory>

    <fileSets>
        <fileSet>
            <directory>${project.build.directory}</directory>
            <outputDirectory>sql-benchmark</outputDirectory>
            <includes>
                <include>*.jar</include>
            </includes>

        </fileSet>
        <fileSet>
            <directory>${basedir.parent}</directory>
            <outputDirectory>sql-benchmark</outputDirectory>
            <includes>
                <include>run.sh</include>
                <include>run.bat</include>
                <include>config.toml</include>
                <include>README.md</include>
            </includes>
            <directoryMode>
                777
            </directoryMode>
        </fileSet>
        <fileSet>
            <directory>${basedir.parent}</directory>
            <outputDirectory>sql-benchmark</outputDirectory>
            <includes>
                <include>jre_**/**</include>
            </includes>
            <directoryMode>
                777
            </directoryMode>
        </fileSet>
    </fileSets>
</assembly>